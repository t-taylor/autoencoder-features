def get_metrics(model, test):
    metrics = ['accuracy', 'tpr', 'tnr', 'ppv']
    output = {}
    for metric in metrics:
        if metric == 'accuracy':
            # TODO
            output[metric] = 'a'
        if metric == 'tpr':
            # TODO
            output[metric] = 'a'
        if metric == 'tnr':
            # TODO
            output[metric] = 'a'
        if metric == 'ppv':
            # TODO
            output[metric] = 'a'

